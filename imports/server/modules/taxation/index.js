import { processTransaction } from './processing'

export default {

    runService: () => {
        // START a schedule for checking input changes?
        // OR connect to a message bus (RxJS) and listen for input changes?

        // THEN process the appropriate transactions based on the changed data.
    }
}