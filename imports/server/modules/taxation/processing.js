
/**
 * Calculates tax-related information from a transaction based on the 
 * configured metadata (tax codes etc).
 * 
 * @param {*} txHash 
 */
const processTransaction = (txHash) => {

}

export default { processTransaction }