import * as actions from '../actions/navigationActions'

const reducer = (state = [], payload) => {
    switch (payload.type) {

        default:
            return state;
    }
};

export default reducer;